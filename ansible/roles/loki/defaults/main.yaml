---
loki_vars:
  kubernetes:
    namespace: monitoring
    helm:
      chart:
        name: loki
        version: 6.12.0
      repository:
        org: grafana
        name: helm-charts
        url: https://grafana.github.io
  alloy:
    helm:
      chart:
        name: alloy
        version: 0.7.0
  promtail:
    helm:
      chart:
        name: promtail
        version: 6.16.0