---
external_secrets_vars:
  kubernetes:
    namespace: kube-system
    helm:
      chart:
        name: external-secrets
        version: "{{ versions.external_secrets }}"
      repository:
        org: external-secrets
        url: https://charts.external-secrets.io