---
ray_vars:
  kubernetes:
    helm:
      repository:
        org: kuberay
        url: https://ray-project.github.io/kuberay-helm/
        github_org: ray-project
      chart:
        version: "{{ versions.ray }}"
    operator:
      helm:
        chart:
          name: kuberay-operator
    namespace: ray
    